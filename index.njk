<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title | default("Background Geek") }}</title>
  <meta name="google" content="notranslate" />
  <link rel="shortcut icon" href="{{ favicon | default('assets/favicon.webp') }}" type="image/x-icon" />
  <link rel="stylesheet" href="{{ stylesheet | default('style.css') }}" />
  {% block head %}{% endblock %}
</head>

<body>
  <div class="container container-1"></div>
  <div class="container container-2"></div>
  {% block body_scripts %}
  <script src="{{ script | default('script.js') }}"></script>
  {% endblock %}
</body>

</html>
